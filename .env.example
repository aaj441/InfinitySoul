# InfinitySol Environment Configuration

# ========== APP SETTINGS ==========
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=info

# ========== SERVER ==========
SERVER_PORT=8000
SERVER_HOST=0.0.0.0
FRONTEND_URL=http://localhost:3000

# ========== DATABASE ==========
DATABASE_URL=postgresql://user:password@localhost:5432/infinitesol
# Optional: SQLite for local development
SQLITE_DB=./infinitesol.db

# ========== BLOCKCHAIN (Polygon/Ethereum) ==========
# For audit logging via smart contract
POLYGON_RPC_URL=https://polygon-rpc.com/
ETHEREUM_RPC_URL=https://eth-rpc.alchemyapi.io/v2/{{your-key}}

# Wallet credentials for publishing audits to blockchain
WALLET_ADDRESS=0x{{your-wallet-address}}
PRIVATE_KEY={{your-private-key-keep-secure}}

# Smart contract address (after deployment)
AUDIT_LOGGER_CONTRACT=0x{{deployed-contract-address}}

# Gas price (in gwei)
GAS_PRICE=50

# ========== IPFS (For immutable audit storage) ==========
IPFS_API_URL=https://ipfs.infura.io:5001
IPFS_HTTP_GATEWAY=https://ipfs.infura.io/ipfs

# Optional: Self-hosted IPFS
# IPFS_API_URL=http://localhost:5001
# IPFS_HTTP_GATEWAY=http://localhost:8080/ipfs

# ========== BLOCKCHAIN VERIFICATION ==========
# Polygonscan/Etherscan for verifying contracts and transactions
POLYGONSCAN_API_KEY={{your-polygonscan-api-key}}
ETHERSCAN_API_KEY={{your-etherscan-api-key}}

# ========== EMAIL SERVICE ==========
SENDGRID_API_KEY={{your-sendgrid-api-key}}
EMAIL_FROM=scanner@infinitesol.com
EMAIL_FROM_NAME=InfinitySol Accessibility Platform

# ========== COURT DATA SOURCES ==========
# CourtListener (Free PACER) API
COURTLISTENER_API_KEY={{optional-for-faster-access}}

# ========== THREAT INTELLIGENCE DATABASE ==========
# File path for SQLite threat database
THREAT_DB_PATH=./threat_intel.db

# Scraping settings
SCRAPE_RATE_LIMIT=100  # requests per minute
SCRAPE_USER_AGENT=InfinitySol-Scanner/1.0
SCRAPE_TIMEOUT=30       # seconds

# ========== AWS (For crisis page deployment) ==========
AWS_ACCESS_KEY_ID={{your-aws-access-key}}
AWS_SECRET_ACCESS_KEY={{your-aws-secret-key}}
AWS_REGION=us-east-1
AWS_S3_BUCKET_PREFIX=crisis-infinitesol

# CloudFront distribution ID (for crisis page CDN invalidation)
CLOUDFRONT_DISTRIBUTION_ID={{your-distribution-id}}

# ========== SECURITY & AUTHENTICATION ==========
JWT_SECRET={{generate-strong-random-secret}}
JWT_EXPIRATION=86400  # 24 hours

# API key for programmatic access
API_KEY={{your-api-key-for-integrations}}

# ========== LEGAL & COMPLIANCE ==========
# UPL (Unauthorized Practice of Law) compliance settings
UPL_DISCLAIMER_REQUIRED=true
INCLUDE_LEGAL_FOOTER=true
REQUIRE_SIGNED_AGREEMENT=true

# Data retention policy (days)
DATA_RETENTION_DAYS=365

# ========== MONITORING & LOGGING ==========
# Sentry error tracking
SENTRY_DSN={{optional-for-error-tracking}}

# LogRocket or similar session replay (development only)
LOGROCKET_ID={{optional}}

# ========== DEVELOPMENT TOOLS ==========
# Enable scanner debugging
SCANNER_DEBUG=false

# Mock blockchain (for local development without spending ETH/MATIC)
USE_MOCK_BLOCKCHAIN=false

# Mock email (print to console instead of sending)
USE_MOCK_EMAIL=false

# ========== FEATURE FLAGS ==========
# Enable experimental features
FEATURE_CRISIS_AUTO_DEPLOY=true
FEATURE_BLOCKCHAIN_LOGGING=true
FEATURE_REAL_TIME_FEED=false
FEATURE_PLAINTIFF_TRACKING=true

# ========== NOTES ==========
# This file should NOT be committed to version control.
# Copy to .env and fill in your actual values.
# For production, use environment variables or a secret manager.
# Example: AWS Secrets Manager, HashiCorp Vault, or GitHub Secrets
